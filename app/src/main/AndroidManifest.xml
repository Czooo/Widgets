<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.demon"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:app="http://schemas.android.com/apk/res-auto"
          xmlns:dist="http://schemas.android.com/apk/distribution">

    <dist:module dist:instant="true"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ViewPagerCompatActivity"/>
        <activity android:name=".FragmentPagerAdapterActivity"/>
        <activity android:name=".NineGridViewActivity"/>
        <activity android:name=".RefreshLayoutActivity"/>
        <activity android:name=".NestedRefreshLayoutActivity"/>
        <activity
            android:name=".SwipeSideLayoutActivity"
            android:theme="@style/SwipeSideActivityTheme"/>
        <activity android:name=".FlowLabelViewActivity"/>
        <activity android:name=".CalendarViewActivity"/>
        <activity android:name=".FixedGridViewActivity"/>
    </application>

</manifest>